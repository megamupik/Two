package Package;

import java.util.*;
import java.sql.*;
import java.util.Date;

import static java.lang.StrictMath.*;
import static java.lang.System.out;

public class Import {
    public static void main(String[] args) {
       Date today;
       out.println("Ferra");
       int x = 2;
       int y = 2;
       int z = (int) sqrt(pow(x, 2) + pow(y, 2));
        out.println(z);
    }
}
